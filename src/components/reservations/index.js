import React from "react";
import SideBar from "../layout/SideBar";
import Helmet from "../layout/Helmet";

function Reservations() {
  return (
    <div>
      <Helmet title="Reservations" />
      <SideBar title="Reservations" />
    </div>
  );
}

export default Reservations;
